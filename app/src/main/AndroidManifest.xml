<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vergjor.android.partyup">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="PartyUp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".DetailedCardActivity"/>
        <activity android:name=".OwnerRegisterActivity"/>
        <activity android:name=".PreLoginActivity"/>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClientRegisterActivity" />
        <activity android:name=".PartyListActivity" />
        <activity android:name=".ClientActivity"/>
        <activity android:name=".ShadowTransformerActivity" />
        <activity android:name=".Activity1"/>

    </application>

</manifest>